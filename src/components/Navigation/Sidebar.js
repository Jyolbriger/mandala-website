import React from "react";

const Sidebar = (props) => {
  return (
    <div
      id="sidebar"
      style={{ ...props.style, backgroundColor: props.colorbg }}
    >
      TEST
    </div>
  );
};

export default Sidebar;
